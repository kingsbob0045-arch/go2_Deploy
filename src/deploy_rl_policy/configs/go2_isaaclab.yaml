# TorchScript created by go2_RL_AMP/isaaclab/scripts/play.py --export.
policy_path: ../go2_RL_AMP/exports/go2_amp.pt
policy_kind: amp  # amp | secamp | rough_residual

# Used only for rough_residual.
motion_prior_path: ../go2_RL_AMP/pretrained/[pos]+[lsgan]+[h2]+[project].pt
residual_action_scale: 0.1
history_steps: 1  # amp/secamp=1; rough_residual=5
default_skill: 0  # SECAMP: pace=0, trot=1, canter=2

# Isaac order: FL, FR, RL, RR; within each leg: hip, thigh, calf.
default_angles: [0.1, 0.8, -1.5, -0.1, 0.8, -1.5, 0.1, 1.0, -1.5, -0.1, 1.0, -1.5]
command_scale: [2.0, 2.0, 0.25]
dof_pos_scale: 1.0
dof_vel_scale: 0.05
action_scale: 0.25
control_dt: 0.02


# Must match Go2AmpEnvCfg.action_clip in go2_RL_AMP (legged_gym clip_actions=100).
action_clip: 100.0
